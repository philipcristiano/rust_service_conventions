name: Pull Request

on:

  pull_request:
    branches:
      - main

jobs:

  # check-for-cc:
  #  runs-on: nixos-latest
  #  steps:
  #    - name: check-for-cc
  #      id: check-for-cc
  #      uses: agenthunt/conventional-commit-checker-action@v2.0.1

  flake:
    runs-on: nixos-runner
    steps:
      - uses: actions/checkout@064fe7f3312418007dea2b49a19844a9ee378f49 #main
      - name: Build
        run: |
            nix develop --command cargo test
            nix develop --command cargo build
