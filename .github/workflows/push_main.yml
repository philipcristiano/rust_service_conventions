name: Main Branch Deploy

on:
  push:
    branches:
      - main

jobs:

  rust_release:
    runs-on: nixos-runner
    steps:

      - name: Checkout repository
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98 #main
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_PLZ_TOKEN }}

      - name: Set Git account
        run: |
          git config user.name "forgejo-actions[bot]"
          git config user.email "forgejo-actions[bot]@example.com"
          BARE_HOST="${FORGEJO_SERVER_URL#https://}"
          BARE_HOST="${BARE_HOST#http://}"
          git config --global url."https://forgejo-actions[bot]:${{ secrets.RELEASE_PLZ_TOKEN }}@${BARE_HOST}".insteadOf "${FORGEJO_SERVER_URL}"

      - name: Run release-plz
        uses: philipcristiano/release-plz-action@b36028081feace3f98562d02fc9752b13f38ef68 # main
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_PLZ_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
          SQLX_OFFLINE: true
        with:
          VERBOSE: true

  flake:
    runs-on: nixos-runner
    steps:
      - uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98 # main
      - name: Build
        run: |
            nix develop --command cargo test --workspace --all-targets --all-features
            nix develop --command cargo test --workspace --doc --all-features
            nix develop --command cargo build --all-targets
